<?xml version="1.0" encoding="utf-8"?>
<odoo>
    
        
        <!--  Produce -->
        
        <record id="view_mrp_product_produce_wizard" model="ir.ui.view">
            <field name="name">MRP Product Produce</field>
            <field name="model">mrp.product.produce</field>
            <field name="arch" type="xml">
                <form string="Produce">
                    <group>
                        <group>
                            <label for="product_qty"/>
                            <div class="o_row">
                                <field name="product_qty" colspan="2"/>
                                <field name="product_uom_id" readonly="1"/>
                            </div>
                            <field name="product_id" invisible="1"/>
                            <field name="tracking" invisible="1"/>
                            <field name="lot_id" domain="[('product_id', '=', product_id)]"
                                        context="{'default_product_id':product_id}"
                                        attrs="{'required': [('tracking', '!=', 'none')]}"
                                        groups="stock.group_production_lot"/>
                        </group>
                        <field name="operation_ids" nolabel="1">
                            <tree>
                                <field name="package_id" groups="stock.group_tracking_lot" invisible="1"/>
                                    <field name="product_id" on_change="product_id_change(product_id, product_uom_id, product_qty)" required="1" attrs="{'readonly': [('fresh_record', '=', False)]}" string="Products Requiring a Lot Number"/>
                                    <field name="fresh_record" invisible="1"/>
                                    <field name="lots_visible" invisible="1"/>
                                    <field name="product_uom_id" attrs="{'readonly': [('fresh_record', '=', False)], 'invisible': [('lots_visible', '=', False)]}" groups="product.group_uom"/>
                                    <field name="owner_id" groups="stock.group_tracking_owner"/>
                                    <field name="location_id" domain="[('id', 'child_of', parent.location_id)]" invisible="1"/>
                                    <field name="location_dest_id" domain="[('id', 'child_of', parent.location_dest_id)]" invisible="1"/>
                                    <field name="from_loc" groups="stock.group_locations,stock.group_tracking_lot"/>
                                    <!-- <field name="to_loc" groups="stock.group_locations,stock.group_tracking_lot"/>
                                    <field name="result_package_id" groups="stock.group_tracking_lot" context="{'location_id': location_dest_id}" invisible="1"/>-->
                                    <field name="state" invisible="1"/>
                                    <field name="production_state" invisible="1"/>
                                    <field name="qty_done" attrs="{'readonly': ['|', ('production_state', '=', 'done'), ('lots_visible', '=', True)]}"/>
                                    <button name="split_lot" string="Lot Split" type="object" icon="fa-list" groups="stock.group_production_lot"
                                            attrs="{'invisible': ['|', ('lots_visible', '=', False), ('state', 'not in', ['confirmed', 'assigned', 'waiting', 'partially_available','done'])]}"/>
                            </tree>
                        </field>
                    </group>
                    <footer>
                        <button name="do_produce" type="object" string="Record Production" class="btn-primary"/>
                        <button string="Cancel" class="btn-default" special="cancel" />
                    </footer>
                </form>
            </field>
        </record>

        <record id="act_mrp_product_produce" model="ir.actions.act_window">
            <field name="name">Produce</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">mrp.product.produce</field>
            <field name="view_type">form</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
        </record>    
        
        
</odoo>
