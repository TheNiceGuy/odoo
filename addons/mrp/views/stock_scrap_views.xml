<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- stock scrap -->
    <record id="view_stock_scrap_form_mrp_inherit" model="ir.ui.view">
        <field name="name">stock.scrap.form.mrp.inherit</field>
        <field name="model">stock.scrap</field>
        <field name="inherit_id" ref="stock.stock_scrap_form_view"/>
        <field name="arch" type="xml">
            <field name="date_expected" position="after">
                <field name="workorder_id" attrs="{'invisible': [('workorder_id', '=', False)]}"/>
                <field name="production_id" attrs="{'invisible': [('production_id', '=', False)]}"/>
            </field>
        </field>
    </record>
</odoo>
